# spec/

Normative, language-agnostic, versioned definitions. This directory is the contract between
the compiler, every SDK, and every host. See `.claude/rules/spec.md` before editing.

Planned contents (Phase 0):

- `graph.schema.json`
- `ir.schema.json`
- `block-manifest.schema.json`
- `wit/packsmith-block.wit`
- `CHANGELOG.md`
