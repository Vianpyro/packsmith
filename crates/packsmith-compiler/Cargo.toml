[package]
name = "packsmith-compiler"
description = "Graph to IR: validation, diagnostics, and version resolution."
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
packsmith-ir = { path = "../packsmith-ir" }
packsmith-blocks = { path = "../packsmith-blocks" }
packsmith-mcversion = { path = "../packsmith-mcversion" }
serde = { workspace = true }
serde_json = { workspace = true }
