[package]
name = "packsmith-ir"
description = "Packsmith IR types and serialization. Pure data, no I/O."
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# This crate is the IR's (de)serialization: serde is the payload, not a
# convenience, and the std has no JSON reader or writer.
serde = { workspace = true }
serde_json = { workspace = true }
