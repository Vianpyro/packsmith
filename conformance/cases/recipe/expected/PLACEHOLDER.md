# expected/ written, not yet verified in game

The files in this directory were produced by building `input.json` with the
current compiler and emitter. They have **not** been confirmed against a real
Minecraft: Java Edition 26.2 instance yet, so the conformance runner still skips
this case (it keys on this file's name).

Remove this file once you have loaded the built pack in 26.2 and confirmed it
loads without a compatibility warning and behaves as `README.md` describes. That
deletion is its own commit.
